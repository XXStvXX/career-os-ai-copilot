# Collector

The collector is currently an early human-in-the-loop module.

It is intended to:

- Receive job candidates from source adapters
- Preserve original source links
- Score candidates against preference rules
- Ask the user to approve, reject, or review later
- Turn user feedback into future filtering signals

The public repo includes only documentation and demo data. Real collector output is excluded.
